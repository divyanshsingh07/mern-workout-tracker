# Production Environment Variables
# Copy this file to .env when deploying

# MongoDB Connection String (Production)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# Server Configuration
NODE_ENV=production
PORT=4000

# CORS Configuration (Add your frontend domains)
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Optional: MongoDB Connection Options
MONGODB_MAX_POOL_SIZE=10
MONGODB_SERVER_SELECTION_TIMEOUT=5000
MONGODB_SOCKET_TIMEOUT=45000

# Example:
# MONGO_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/workoutdb?retryWrites=true&w=majority
# FRONTEND_URL=https://my-workout-app.vercel.app
