# MongoDB Connection String Template
# Copy this file to .env and replace the placeholder values

# Replace <username>, <password>, <cluster-url>, and <database-name> with your actual values
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# Server Port (optional - defaults to 3000)
PORT=3000

# Example:
# MONGO_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/workoutdb?retryWrites=true&w=majority

